<Window x:Class="ProgressImplementer.UI.Windows.ProgressWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="600" SizeToContent="Height" ResizeMode="NoResize"
             WindowStartupLocation="CenterOwner"
             Title="Окно с прогрессом">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="Далее здесь будут какие-либо настройки прогресса" Margin="5" />

        <!-- ProgressBar -->

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="ОК" Width="75" Margin="0 0 5 0" />
            <Button Content="Отмена" Width="75" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>